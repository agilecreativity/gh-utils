(ns gh_utils.core-test
  (:require [clojure.test :refer :all]
            [com.agilecreativity.gh_utils.core :as ghu]))

(deftest simple-test-example
  (testing "Simple test place holder"
    (is (= 6 (+ 2 4)))))

;; To run the test
(run-tests)
